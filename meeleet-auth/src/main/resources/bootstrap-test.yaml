spring:
  cloud:
    nacos:
      discovery:
        namespace: test
        group: LEARN_GROUP
        server-addr: nacos-headless.infrastructure:8848   # 使用k8s无头服务
      config:
        namespace: test
        group: LEARN_GROUP
        file-extension: yaml
        prefix: ${spring.application.name}
        shared-configs[0]:
          group: LEARN_GROUP
          data-id: learn-common.yaml
          refresh: true
